{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/cleanup-dev-instance.js && cross-env MULTIPLAYER_MODE=false STATIC_PORT=8000 GAME_PORT=8080 ENABLE_POLICY_SERVER=false nodemon --ext ts --signal SIGTERM --exec ts-node main.ts",
    "build": "tsc",
    "start:multiplayer": "cross-env MULTIPLAYER_MODE=true ENABLE_POLICY_SERVER=true node dist/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "cross-env": "^10.0.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}
